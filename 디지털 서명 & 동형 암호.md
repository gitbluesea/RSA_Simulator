# RSA 고급 기능 파이썬 시뮬레이터 (디지털 서명 & 동형 암호)

이 문서는 RSA의 핵심 수학적 성질인 **디지털 서명(Digital Signature)**과 **곱셈적 동형 암호(Homomorphic Encryption)**를 직접 검증할 수 있는 파이썬 예제 코드와 해설입니다.

* **테스트 기본 키 세팅:** \(p=61, q=53 \rightarrow n=3233, e=17, d=2753\)

---

## 1. 디지털 서명 (Digital Signature) 실습
디지털 서명은 **"개인 키로 암호화한 것은 오직 쌍이 되는 공개 키로만 복호화할 수 있다"**는 원리를 이용합니다.

### 소스 코드
```python
n = 3233
e = 17   # 공개 키 (검증용)
d = 2753 # 개인 키 (서명용)

# 1. 송신자가 메시지에 서명
message = 123  
signature = pow(message, d, n)  # 개인 키 d를 사용해 서명 생성

print("=== [디지털 서명 생성] ===")
print(f"원본 메시지: {message}")
print(f"생성된 디지털 서명: {signature}\n")

# 2. 수신자가 송신자의 공개 키로 서명 검증
decrypted_message = pow(signature, e, n)  # 공개 키 e를 사용해 서명 해독

print("=== [디지털 서명 검증] ===")
print(f"서명을 해독한 결과: {decrypted_message}")
if message == decrypted_message:
    print("[검증 성공] 이 서명은 올바른 개인 키로 작성된 진짜 서명입니다!")
else:
    print("[검증 실패] 서명이 위조되었거나 키가 일치하지 않습니다.")
```

---

## 2. 곱셈적 동형 암호 (Homomorphic Encryption) 실습
데이터를 **암호화된 상태 그대로 곱해도, 복호화하면 원본 데이터를 곱한 것과 같은 결과**가 유지되는 성질입니다.

### 소스 코드
```python
n = 3233
e = 17
d = 2753

# 두 개의 민감한 데이터 가정
M1 = 5
M2 = 4

# 1. 데이터를 각각 암호화 (사용자 환경)
C1 = pow(M1, e, n)
C2 = pow(M2, e, n)

print("=== [사용자: 데이터 암호화] ===")
print(f"평문 데이터: M1 = {M1}, M2 = {M2}")
print(f"암호화 데이터: C1 = {C1}, C2 = {C2}\n")

# 2. 암호화된 상태로 클라우드 서버에서 곱셈 연산 수행
# 서버는 원본 데이터(5, 4)를 모른 채 암호문끼리만 연산합니다.
C_multiplied = (C1 * C2) % n

print("=== [클라우드 서버: 암호문 상태로 연산] ===")
print(f"암호문 곱셈 결과 (C1 * C2 % n): {C_multiplied}\n")

# 3. 사용자가 연산 결과를 다운로드받아 복호화
M_result = pow(C_multiplied, d, n)

print("=== [사용자: 결과 복호화] ===")
print(f"암호문 연산 결과를 복호화한 값: {M_result}")
print(f"실제 원본 데이터의 곱 (5 × 4): {M1 * M2}")

if M_result == (M1 * M2):
    print("[성공] 데이터를 노출하지 않고 클라우드가 정확한 곱셈 연산을 수행했습니다!")
```
