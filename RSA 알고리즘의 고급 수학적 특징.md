# [정수론] RSA 알고리즘의 고급 수학적 특징

RSA 알고리즘은 순수한 정수론(Number Theory)과 추상대수학의 결합으로 완성된 암호체계입니다. 단순 소인수분해의 난해성 외에 학술적·실무적으로 중요한 5가지 수학적 특징을 정리합니다.

## 1. 암호화와 디지털 서명의 대칭성
RSA는 **개인 키로 먼저 연산하고 공개 키로 나중에 연산해도 원래대로 돌아오는** 역연산의 유연성을 가집니다.
* **일반 암호화:** 평문 $M$을 공개 키 $e$로 암호화 후 개인 키 $d$로 복호화
  $$\left(M^e\right)^d \equiv M \pmod n$$
* **디지털 서명:** 평문 $M$을 개인 키 $d$로 서명(암호화) 후 공개 키 $e$로 검증(복호화)
  $$\left(M^d\right)^e \equiv M \pmod n$$

## 2. 곱셈적 상동성 (Multiplicative Homomorphism)
암호문 상태에서 곱셈 연산을 수행하면, 평문 상태에서 곱한 것과 수학적으로 동일한 결과가 도출됩니다.
* 두 암호문 $C_1 = M_1^e \pmod n$, $C_2 = M_2^e \pmod n$의 곱:
  $$C_1 \times C_2 = (M_1^e) \times (M_2^e) = (M_1 \times M_2)^e \pmod n$$
* **의의:** 데이터를 복호화하지 않고 암호화된 상태 그대로 연산할 수 있는 **동형 암호(Homomorphic Encryption)**의 시초가 됩니다.

## 3. 선택 암호문 공격(CCA) 취약점
곱셈적 상동성은 수학적으로 우아하지만, 실무 보안에서는 취약점이 될 수 있습니다.
* 공격자가 임의의 숫자 $X$를 골라 가짜 암호문 $C' = C \times X^e \pmod n$을 주입한 뒤, 복호화 시스템을 통해 얻은 결과값에 $X$의 역원을 곱하면 실제 평문 $M$을 알아낼 수 있습니다.
* **해결책:** 현대 RSA는 평문에 무작위 데이터를 강제로 섞는 **OAEP(Optimal Asymmetric Encryption Padding)** 기술을 결합하여 이 수학적 고정성을 깨뜨립니다.

## 4. 오일러의 정리 (Euler's Theorem)의 직접적 응용
RSA 작동을 보장하는 근본적인 지지대는 오일러의 정리입니다.
* **정리:** $a$와 $n$이 서로소일 때, $a^{\phi(n)} \equiv 1 \pmod n$이 성립합니다.
* 키 쌍이 $e \times d = k \cdot \phi(n) + 1$을 만족하므로, 암·복호화 시 아래와 같이 전개되어 복원이 보장됩니다.
  $$M^{ed} = M^{k \cdot \phi(n) + 1} = (M^{\phi(n)})^k \times M^1 \equiv 1^k \times M \equiv M \pmod n$$

## 5. 고정점 (Fixed Point)의 존재
모듈러 거듭제곱 연산의 특성상, 어떠한 키 쌍을 사용하더라도 **암호화를 가해도 숫자가 전혀 변하지 않는 평문**이 수학적으로 반드시 존재합니다.
* 대표적으로 $M = 0, 1, n-1$은 어떤 공개 지수 $e$승을 해도 언제나 자기 자신으로 암호화됩니다. ($0^e \equiv 0$, $1^e \equiv 1$)
